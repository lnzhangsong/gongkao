node_modules
dist
.npm-cache
*.local
.DS_Store
.vercel
.env*
!.env.example

# 数据库是**构建产物**：data/ 下的可读源由 vp run db:rebuild（或 build / test 自动触发）生成它
data/articles.db
# SQLite WAL 临时文件
data/*.db-shm
data/*.db-wal

# Command Code 工具自身配置（taste / settings / plans 等，不随项目提交）
.commandcode/
.zcode/
__pycache__/
*.pyc
